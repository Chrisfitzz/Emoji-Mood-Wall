{
  "name": "emoji-mood-wall-root",
  "private": true,

  "scripts": {
    "dev": "cd backend && npm run dev",
    "start": "cd backend && npm run start",
    "build": "cd backend && npm run build"
  }
}
